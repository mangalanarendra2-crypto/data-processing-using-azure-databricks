# For local development/testing only.
# On Azure Databricks, PySpark and Delta Lake are already provided by the runtime —
# do NOT pip-install pyspark/delta-spark inside a Databricks notebook.
pyspark==3.5.1
delta-spark==3.1.0
pyyaml>=6.0
faker>=24.0.0
pytest>=8.0.0
